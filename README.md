# Movie Locker Bot — Bot Only Channel Management

در این نسخه دیگر لازم نیست کانال‌های اجباری را داخل Railway Variables وارد کنی.
ادمین همه کانال‌های اجباری را از داخل خود ربات مدیریت می‌کند.

## Railway Variables لازم

فقط این‌ها لازم است:

```env
BOT_TOKEN=توکن ربات
ADMIN_IDS=آیدی عددی ادمین
DATABASE_URL=sqlite+aiosqlite:////app/data/bot.db
DELETE_AFTER_SECONDS=15
SKIP_MEMBERSHIP_FOR_ADMINS=1
```

دیگر لازم نیست این‌ها را در Railway بزنی:

```env
REQUIRED_CHANNEL
REQUIRED_CHANNEL_LINK
```

## مدیریت کانال‌ها داخل ربات

نمایش پنل کانال‌ها:

```text
/channels
```

افزودن کانال عمومی:

```text
/addchannel @irfreenet
```

جایگزینی همه کانال‌های قبلی با یک کانال:

```text
/setchannel @irfreenet
```

حذف کانال:

```text
/delchannel1
```

برای کانال خصوصی:

```text
/addchannel -1001234567890 https://t.me/+InviteLink
```

## دکمه شیشه‌ای عضویت

برای کانال عمومی، وقتی با `/addchannel @username` اضافه شود، ربات خودش لینک شیشه‌ای را می‌سازد:

```text
https://t.me/username
```

برای کانال خصوصی، چون یوزرنیم عمومی ندارد، باید لینک دعوت را بدهی.

## نکته مهم

ربات باید داخل هر کانال اجباری Admin باشد تا بتواند عضویت کاربران را بررسی کند.

## آپلود نکن

```text
.env
bot.db
.venv
__pycache__
```


## منوی شناور / برای ادمین

در این نسخه وقتی ادمین داخل ربات `/` بزند، دستورهای مدیریتی را به صورت شناور می‌بیند:

```text
/add
/files
/stats
/channels
/addchannel
/setchannel
/delchannel
/debug_channel
```

نکته: خود تلگرام فقط نام دستور را در منوی شناور نشان می‌دهد، نه آرگومان کامل.  
برای مثال بعد از انتخاب `/addchannel` باید ادامه‌اش را خودت بنویسی:

```text
/addchannel @irfreenet
```

یا برای کانال خصوصی:

```text
/addchannel -1001234567890 https://t.me/+InviteLink
```


## نسخه movie-section-gate-v1

در این نسخه برای کاربران چهار دکمه اصلی اضافه شده است:

- 📢 کانال تلگرام
- 🔐 فیلترشکن پرمیوم رایگان
- 💎 دریافت اکانت های پولی سایت های معروف
- ☎️ ارتباط با تیم ما

هر دکمه کانال‌های عضویت اجباری جداگانه خودش را دارد.

کلیدهای هر بخش:

- `telegram` برای کانال تلگرام
- `vpn` برای فیلترشکن پرمیوم رایگان
- `accounts` برای بخش اکانت‌ها
- `contact` برای ارتباط با تیم ما

نمونه افزودن کانال برای فیلترشکن:

```text
/addsectionchannel vpn @channel1
/addsectionchannel vpn @channel2
/addsectionchannel vpn @channel3
/addsectionchannel vpn @channel4
```

نمونه افزودن کانال برای اکانت‌ها:

```text
/addsectionchannel accounts @channelA
/addsectionchannel accounts @channelB
```

برای دیدن لیست کانال‌های اختصاصی:

```text
/sectionchannels
```

برای حذف کانال اختصاصی:

```text
/delsectionchannel 1
```

نکته: برای بخش اکانت‌ها فقط اطلاعات یا لینک‌هایی را قرار بده که مجوز انتشارش را داری.


## نسخه v2 — دستورهای سریع ادمین برای افزودن کانال

تلگرام اجازه نمی‌دهد متن کامل همراه آرگومان به صورت خودکار داخل باکس تایپ نوشته شود. برای همین در این نسخه دستورهای کوتاه جدا اضافه شده‌اند. ادمین وقتی `/` را بزند این دستورها را می‌بیند:

- `/addvpnchannel`
- `/addaccountchannel`
- `/addtelegramchannel`
- `/addcontactchannel`

روش استفاده سریع:

```text
/addvpnchannel @channel1
/addaccountchannel @channel2
/addtelegramchannel @channel3
/addcontactchannel @channel4
```

اگر ادمین فقط خود دستور را بفرستد، ربات مرحله بعدی را باز می‌کند و فقط اسم کانال را می‌پرسد. برای کانال خصوصی هم این قالب را بفرست:

```text
-1001234567890 https://t.me/+InviteLink
```

بعد از Deploy داخل Railway Logs باید این عبارت دیده شود:

```text
Bot started — movie-section-gate-v2-quick-admin-commands
```


## نسخه v3 — گیت عمومی در /start و ارتباط بدون عضویت جدا

در این نسخه وقتی کاربر `/start` می‌زند، ابتدا کانال‌های اجباری عمومی نمایش داده می‌شود. بعد از تایید عضویت، منوی کاربر نمایش داده می‌شود.

دکمه‌های کاربر:
- 📢 کانال تلگرام
- 🔐 فیلترشکن پرمیوم رایگان
- 💎 دریافت اکانت های پولی سایت های معروف
- ☎️ ارتباط با تیم ما

گزینه ارتباط با تیم ما عضویت اجباری اختصاصی ندارد.

مدیریت کانال عمومی شروع:
- `/addchannel @YourChannel`
- `/channels`
- `/delchannel 1`
- `/debug_channel`

نکته مهم درباره منوی `/` تلگرام:
تلگرام اجازه نمی‌دهد ربات کل دستور را داخل Write Message به صورت سفارشی پر کند. وقتی دستور `/addchannel` خالی ارسال شود، هیچ کانالی اضافه نمی‌شود و فقط نمونه دستور نمایش داده می‌شود. ادمین باید بعد از دستور، نام کانال را اضافه کند؛ مثال: `/addchannel @YourChannel`.

بعد از Deploy، داخل Railway Logs باید این عبارت را ببینی:
`Bot started — movie-start-gate-v3-contact-free`


## نسخه v4 — افزودن کانال اجباری از منوی ادمین

در این نسخه ادمین لازم نیست دستور کامل را حفظ کند.

از منوی ادمین گزینه `➕ افزودن کانال اجباری` را بزن، سپس یکی از بخش‌ها را انتخاب کن:

- عضویت اجباری شروع ربات
- فیلترشکن پرمیوم رایگان
- اکانت های پولی سایت های معروف
- کانال تلگرام

بعد فقط یوزرنیم کانال را بفرست:

```text
@YourChannel
```

برای کانال خصوصی:

```text
-1001234567890 https://t.me/+InviteLink
```

گزینه `☎️ ارتباط با تیم ما` عضویت اجباری جداگانه ندارد.

برای تست نسخه در Railway Logs باید این متن دیده شود:

```text
Bot started — movie-start-gate-v4-admin-section-channel-menu
```


## نسخه v5 — پنل شیشه‌ای مدیریت کانال‌های اجباری

در این نسخه ادمین لازم نیست دستور کامل را حفظ کند.

از منوی ادمین دکمه زیر را بزن:

```text
➕ افزودن کانال اجباری
```

بعد ربات می‌پرسد این کانال برای کدام بخش اضافه شود:

- عضویت اجباری شروع ربات
- فیلترشکن پرمیوم رایگان
- دریافت اکانت های پولی سایت های معروف
- کانال تلگرام

بعد از انتخاب بخش، ادمین فقط یوزرنیم کانال را می‌فرستد:

```text
@YourChannel
```

برای مدیریت کانال‌ها هم دکمه زیر را بزن:

```text
📢 کانال‌های اجباری
```

ربات اول بخش‌ها را نشان می‌دهد. بعد از انتخاب هر بخش، لیست کانال‌های همان بخش نمایش داده می‌شود و برای هر کانال گزینه‌های زیر وجود دارد:

- ادیت
- حذف
- افزودن کانال جدید

گزینه ارتباط با تیم ما عضویت اجباری جداگانه ندارد.

بعد از Deploy در Railway Logs باید این عبارت را ببینی:

```text
Bot started — movie-admin-channel-manager-v5
```


## نسخه v6 — رفع لینک فایل و پاکسازی دستورهای شناور ادمین

تغییرات:
- لینک فایل‌های ثبت‌شده دیگر به `REQUIRED_CHANNEL_LINK` وابسته نیست.
- وقتی کاربر روی لینک فایل می‌زند، اگر عضو کانال عمومی نباشد همان کانال‌های اجباری عمومی دیتابیس نمایش داده می‌شود.
- خطای «لینک عضویت کانال تنظیم نشده است» برای لینک فایل حذف شد.
- دستورهای اضافه روش قبلی از منوی شناور `/` ادمین حذف شدند.
- منوی شناور ادمین فقط این موارد را نشان می‌دهد:
  - `/start`
  - `/help`
  - `/add`
  - `/files`
  - `/stats`
  - `/channels`
  - `/addchannel`

نکته:
برای اضافه کردن کانال عمومی شروع ربات از داخل خود ربات استفاده کن:
`/addchannel`
بعد بخش «عضویت اجباری شروع ربات» را انتخاب کن و یوزرنیم کانال را بفرست.


## نسخه v7 — اصلاح دریافت فایل و پاک‌سازی دستورهای قدیمی

تغییرات مهم:
- لینک فایل با `?start=f_ID` از همان کانال‌های عضویت اجباری شروع ربات استفاده می‌کند.
- دیگر برای لینک فایل به `REQUIRED_CHANNEL_LINK` نیاز نیست.
- کاربر چه `/start` خالی بزند، چه از لینک فایل وارد شود، همان عضویت اجباری عمومی را می‌بیند.
- دکمه تکراری «کانال‌های بخش‌ها» از منوی ادمین حذف شد.
- دستورهای قدیمی روش قبلی مثل `/addvpnchannel`، `/addsectionchannel` و `/sectionchannels` از منوی شناور `/` پاک می‌شوند.
- اگر ادمین دستور قدیمی را دستی بفرستد، ربات به پنل جدید مدیریت کانال‌ها راهنمایی می‌کند.

بعد از Deploy، در Railway Logs باید ببینی:
`Bot started — movie-file-gate-commands-clean-v7`

نکته:
اگر تلگرام همچنان دستورهای قدیمی را در منوی `/` نشان داد، یک بار چت ربات را ببند و باز کن یا چند دقیقه صبر کن؛ تلگرام گاهی Command Menu را کش می‌کند.


## نسخه v7.1 — اصلاح دکمه مدیریت کانال‌های اجباری

در این نسخه دکمه «➕ مدیریت و افزودن کانال اجباری» مستقیماً پنل کامل مدیریت کانال‌ها را باز می‌کند.

مسیر کار:
1. دکمه «➕ مدیریت و افزودن کانال اجباری» یا «📢 کانال‌های اجباری» را بزن.
2. بخش مورد نظر را انتخاب کن.
3. داخل همان بخش، لیست کانال‌ها را می‌بینی.
4. کنار هر کانال دکمه‌های «✏️ ادیت» و «🗑 حذف» هست.
5. پایین همان صفحه دکمه «➕ افزودن کانال جدید» هست.

دستور کمکی:
- `/cancel` برای خروج از حالت ثبت فایل یا ثبت کانال
- `/version` برای بررسی نسخه فعال

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.1-channel-manager-button-fix`


## نسخه v7.2 — مدیریت فایل‌ها با دکمه

در این نسخه بخش «📋 لیست فایل‌ها» برای هر فایل دکمه دارد:

- ✏️ ادیت عنوان
- ⛔️ غیرفعال کردن / ✅ فعال کردن
- 🗑 حذف کامل

نکته:
- غیرفعال کردن یعنی لینک فایل باقی می‌ماند، اما کاربر فایل را دریافت نمی‌کند.
- حذف کامل یعنی رکورد فایل از دیتابیس حذف می‌شود و لینک آن دیگر کار نمی‌کند.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.2-file-manager-buttons`

برای تست نسخه:
`/version`


## نسخه v7.3 — دکمه بکاپ دیتابیس

در این نسخه دکمه و دستور بکاپ اضافه شد:

- دکمه: `💾 بکاپ دیتابیس`
- دستور: `/backup`

بکاپ شامل این موارد است:
- کانال‌های عضویت اجباری عمومی و بخش‌ها
- فایل‌های ثبت‌شده و لینک‌های آن‌ها
- وضعیت فعال/غیرفعال فایل‌ها
- کاربران ثبت‌شده

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.3-backup-button`

برای تست نسخه:
`/version`


## نسخه v7.4 — بکاپ خودکار دیتابیس

این نسخه علاوه بر دکمه «💾 بکاپ دیتابیس»، بکاپ خودکار هم دارد.

Variables جدید Railway:

```env
AUTO_BACKUP_ENABLED=1
AUTO_BACKUP_HOURS=24
AUTO_BACKUP_ON_START=0
```

توضیح:
- `AUTO_BACKUP_ENABLED=1` بکاپ خودکار را فعال می‌کند.
- `AUTO_BACKUP_HOURS=24` هر ۲۴ ساعت بکاپ را برای ادمین‌ها می‌فرستد.
- `AUTO_BACKUP_ON_START=1` باعث می‌شود بعد از هر Deploy هم یک بکاپ ارسال شود. اگر زیاد Deploy می‌زنی، بهتر است `0` بماند.

دستور دستی:
`/backup`

دکمه ادمین:
`💾 بکاپ دیتابیس`

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.4-auto-backup`

نکته امنیتی:
فایل `bot_backup.db` را داخل GitHub عمومی آپلود نکن.


## نسخه v7.4.1 — بکاپ خودکار بدون دکمه visible

در این نسخه دکمه «💾 بکاپ دیتابیس» از منوی تلگرام حذف شده است.

بکاپ خودکار همچنان فعال است و با این Variables کنترل می‌شود:

```env
AUTO_BACKUP_ENABLED=1
AUTO_BACKUP_HOURS=24
AUTO_BACKUP_ON_START=0
```

دستور دستی مخفی هنوز کار می‌کند:
`/backup`

ولی این دستور در منوی شناور `/` نمایش داده نمی‌شود و دکمه‌ای هم در منوی ادمین ندارد.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.4.1-auto-backup-hidden`


## نسخه v7.5 — اضافه شدن دکمه‌های لینک مستقیم

در این نسخه دو دکمه کاربری اضافه/اصلاح شد:

- `☎️ ارتباط با تیم ما`
- `💰 کسب درآمد`

هر دو به این لینک وصل هستند:
`https://t.me/NeoSeoTeam`

نکته:
دکمه‌های Reply Keyboard تلگرام مستقیم URL باز نمی‌کنند؛ بنابراین وقتی کاربر این گزینه‌ها را می‌زند، ربات یک دکمه شیشه‌ای لینک‌دار ارسال می‌کند.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.5-extra-user-links`


## نسخه v7.6 — دکمه مستقیم به پیوی تیم

در این نسخه منوی کاربر به صورت دکمه‌های شیشه‌ای نمایش داده می‌شود.

مزیت:
- دکمه `☎️ ارتباط با تیم ما` مستقیم لینک `https://t.me/NeoSeoTeam` را باز می‌کند.
- دکمه `💰 کسب درآمد` مستقیم لینک `https://t.me/NeoSeoTeam` را باز می‌کند.
- دیگر برای این دو گزینه پیام واسطه ارسال نمی‌شود.

نکته:
تلگرام اجازه نمی‌دهد دکمه‌های منوی پایین Reply Keyboard مستقیم URL باز کنند. برای باز شدن مستقیم پیوی، دکمه باید Inline URL باشد. این نسخه همین کار را انجام می‌دهد.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.6-direct-team-buttons`


## نسخه v7.7 — دکمه بکاپ داخل ربات

این نسخه همان دکمه‌های مستقیم `ارتباط با تیم ما` و `کسب درآمد` را نگه می‌دارد و دکمه بکاپ دیتابیس را هم به منوی ادمین اضافه می‌کند.

دکمه ادمین:
`💾 بکاپ دیتابیس`

دستور دستی:
`/backup`

بعد از زدن دکمه یا دستور، ربات فایل دیتابیس را برای ادمین می‌فرستد.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.7-direct-buttons-backup`

نکته امنیتی:
فایل `bot_backup.db` را داخل GitHub عمومی آپلود نکن.


## نسخه v7.8 — ساخت پست دکمه‌دار برای کانال

این نسخه یک دکمه ادمین اضافه می‌کند:

`📤 ساخت پست کانال`

جریان کار:
1. ادمین دکمه «📤 ساخت پست کانال» را می‌زند.
2. از لیست فایل‌های ثبت‌شده یک فایل انتخاب می‌کند.
3. متن پست کانال را می‌فرستد.
4. عکس، ویدیو، گیف یا فایل پست را می‌فرستد، یا `/skip` می‌زند.
5. ربات پست را داخل کانال اصلی منتشر می‌کند.

دکمه‌های زیر پست:
- `🎬 دریافت فایل` → لینک فایل داخل ربات
- `☎️ ارتباط با تیم ما` → `https://t.me/NeoSeoTeam`
- `💰 کسب درآمد` → `https://t.me/NeoSeoTeam`

Variable جدید Railway:

```env
POST_CHANNEL_ID=@YourMainChannel
```

یا اگر کانال خصوصی/بدون یوزرنیم است:

```env
POST_CHANNEL_ID=-1001234567890
```

نکته مهم:
ربات باید داخل کانال اصلی ادمین باشد و اجازه ارسال پست داشته باشد.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.8-channel-post-builder`


## نسخه v7.9 — ساخت پست با دکمه دلخواه

در این نسخه دکمه‌های ثابت زیر پست حذف شدند.

جریان جدید:
1. ادمین می‌زند: `📤 ساخت پست کانال`
2. اگر خواست، یکی از فایل‌های ثبت‌شده را انتخاب می‌کند.
3. متن پست را می‌فرستد.
4. عکس، ویدیو، گیف یا فایل پست را می‌فرستد، یا `/skip` می‌زند.
5. متن دکمه شیشه‌ای را می‌فرستد، مثلا `🎬 دریافت فایل`.
6. لینک دکمه را می‌فرستد.

لینک دکمه می‌تواند یکی از این‌ها باشد:
- لینک کامل مثل `https://t.me/NeoSeoTeam`
- لینک فایل داخل ربات مثل `https://t.me/YourBot?start=f_12`
- فقط آیدی فایل مثل `12`
- اگر اول فایل ثبت‌شده انتخاب شده باشد، با `/skip` همان لینک فایل استفاده می‌شود.

زیر پست فقط همان یک دکمه‌ای می‌آید که ادمین ساخته است.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v7.9-custom-post-button`


## نسخه v8.0 — انتخاب کانال مقصد هنگام ساخت پست

در نسخه‌های قبلی، پست فقط به کانال ثابت `POST_CHANNEL_ID` ارسال می‌شد.

در این نسخه ادمین در مرحله آخر تعیین می‌کند پست به کدام کانال یا کانال‌ها ارسال شود.

مثال یک کانال:
`@YourChannel`

مثال چند کانال:
`@Channel1 @Channel2 -1001234567890`

اگر در Railway مقدار `POST_CHANNEL_ID` تنظیم شده باشد، ادمین می‌تواند در مرحله کانال مقصد فقط بزند:
`/default`

نکته مهم:
ربات باید داخل همه کانال‌های مقصد ادمین باشد و اجازه ارسال پست داشته باشد.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v8.0-post-target-channels`


## نسخه v8.1 — مشاهده لیست پست‌های منتشرشده

در این نسخه بعد از اینکه ربات پست را داخل کانال منتشر می‌کند، اطلاعات آن در دیتابیس ذخیره می‌شود.

دکمه جدید ادمین:
`📋 لیست پست‌های کانال`

دستور:
`/posts`

در لیست پست‌ها این موارد نمایش داده می‌شود:
- آیدی داخلی پست
- کانال مقصد
- Message ID پیام داخل کانال
- فایل متصل، اگر وجود داشته باشد
- متن دکمه
- خلاصه متن پست
- لینک مشاهده پست، اگر قابل ساخت باشد

برای کانال‌های عمومی با `@username` لینک مستقیم ساخته می‌شود.
برای کانال‌های خصوصی با آیدی `-100...` لینک `t.me/c/...` ساخته می‌شود که فقط برای اعضای همان کانال باز می‌شود.

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v8.1-post-list`


## نسخه v8.2 — حفظ لینک داخل متن پست

در نسخه‌های قبلی، متن پست با `parse_mode=None` ارسال می‌شد و اگر ادمین داخل متن یک کلمه را لینک‌دار می‌کرد، لینک داخل کانال از بین می‌رفت.

در این نسخه:
- لینک‌هایی که ادمین داخل خود Telegram روی متن می‌گذارد حفظ می‌شوند.
- متن‌های HTML مثل `<a href="https://example.com">سایت ما</a>` پشتیبانی می‌شوند.
- فرمت ساده Markdown مثل `[سایت ما](https://example.com)` هم به لینک HTML تبدیل می‌شود.
- ارسال پست به کانال با `ParseMode.HTML` انجام می‌شود.

روش پیشنهادی داخل مرحله «متن پست»:

`<a href="https://example.com">سایت ما را دنبال کنید</a>`

یا:

`[سایت ما را دنبال کنید](https://example.com)`

بعد از Deploy باید در Railway Logs ببینی:
`Bot started — movie-bot-v8.2-html-links`
